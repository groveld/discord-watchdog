<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Run in full-screen mode. -->
    <meta name="apple-mobile-web-app-capable" content="yes">

    <!-- Make the status bar black with white text. -->
    <meta name="apple-mobile-web-app-status-bar-style" content="black">

    <!-- Customize home screen title. -->
    <meta name="apple-mobile-web-app-title" content="Web App">

    <!-- Disable phone number detection. -->
    <meta name="format-detection" content="telephone=no">

    <!-- Set viewport. -->
    <meta name="viewport" content="initial-scale=1">

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css" integrity="sha384-9gVQ4dYFwwWSjIDZnLEWnxCjeSWFphJiwGPXr1jddIhOegiu1FwO5qRGvFXOdJZ4" crossorigin="anonymous">
    <link rel="stylesheet" href="/public/css/dashboard.css" />

    <title><%= bot.user.username %>: Discord Bot</title>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark mb-3" style="background-color: #34383c">
      <a class="navbar-brand" href="/">
        <img src="<%= bot.user.avatarURL %>" width="30" height="30" class="d-inline-block align-top rounded-circle mr-1" alt="">
        <%= bot.user.username %>
      </a>

      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarMain" aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarMain">
        <div class="navbar-nav mr-auto">
          <a class="nav-item nav-link" href="/stats">Stats</a>
          <a class="nav-item nav-link" href="/commands">Commands</a>
        </div>

        <div class="dropdown">
          <% if(user) { %>
          <button class="btn btn-link dropdown-toggle" type="button" id="dropdownMenu" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= user.username %></button>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenu">
            <a class="dropdown-item" href="/dashboard">My servers</a>
            <a class="dropdown-item" href="/logout">Logout</a>
          </div>
          <% } else { %>
          <a href="https://discord.gg/2dV4xRN" class="btn btn-sm btn-outline-secondary" role="button" aria-pressed="true">Join</a>
          <a href="/login" class="btn btn-sm btn-outline-success" role="button" aria-pressed="true">Login</a>
          <% } %>
        </div>

      </div>
    </nav>
